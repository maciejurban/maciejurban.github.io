---
title: Many empty states are missing domain states
tension: >-
  The link was valid, the survey existed, and the screen said the link was
  broken.
kind: mini
order: 5
role: '[TK — needs source]'
period: '[TK — needs source]'
context: '[[ORG]]'
themes:
  - semantics-before-ui
relatedEssays: []
status: draft
summary: >-
  Unverified. Held back pending confirmation of the details, so this one is not
  written and not published.
updated: 2026-09-01
---

{/*
  Deliberately unwritten.

The details of this one have not been verified, so there is nothing here to
publish and nothing here to check. It stays `status: draft`, which means it
renders in `npm run dev` and is absent from the build. Nothing else on the site
links to it.

To write it: confirm the sequence of events and what the screen actually said,
then replace this stub. Until then the file exists only so the slug is reserved
and the shape of the case set is visible while writing. */}

Not written yet. The details are unverified, so this case is held back rather
than reconstructed from memory.